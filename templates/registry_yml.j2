registry:
  restart: always
  image: registry:2
  ports:
    - 4000:5000
  environment:
    REGISTRY_HTTP_TLS_CERTIFICATE: /certs/star_edisonqa.sgns.net.crt
    REGISTRY_HTTP_TLS_KEY: /certs/star_edisonqa.sgns.net.key
    REGISTRY_HTTP_SECRET: thisisasecretthatshouldbebetter
  volumes:
    - /var/lib/docker/registry:/var/lib/registry
    - /etc/ssl/certs:/certs

