DEVICE=eth0
BOOTPROTO=none
ONBOOT=yes
HWADDR={% for key,value in physical.iteritems() %}{% if value.name | match (inventory_hostname) %}{{value.mac_0}}{% endif %}{% endfor %}
MASTER=bond0
SLAVE=yes
MTU=9000
